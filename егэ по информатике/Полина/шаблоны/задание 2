print("x y w z | F") #выводим, чтобы у нас появилась табличка
for x in range(2): #range(2) потому что xywz могут принимать только значения 1 и 0
    for y in range(2):
        for w in range(2):
            for z in range(2):
                F = ... #переписываем из условия задачи (≡ это ==,  -> это <=,  ^ это and,  ∨ это or )
                if F == ... #пишем из условия чему равна F (1 или 0)
                print(x, y, w, z, "|", int(F)) #int чтобы F выводилось как число, а не True или False